#!/usr/bin/env bash

# Scale deployment
kubectl scale deployment django-messaging --replicas=3

# Verify pods
kubectl get pods

# Load testing (dummy)
wrk http://localhost || true

# Monitor resources
kubectl top pods || true
